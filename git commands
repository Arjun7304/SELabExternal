
git --version
git config --global user.name "Your Name“
git config --global user.email "your.email@example.com"
git init 
git status
git add .
git commit -m "Added new feature"
git diff

git branch <branch_name>: Creates a new branch.
git branch -a: Lists all branches.
git branch -d <branch_name>: Deletes a branch.
git checkout <branch_name>: Switch to an existing branch.
git checkout -b <new_branch>: Create and switch to a new branch in one step.
git merge <branch_name>: Combines the changes from the specified branch
ls
git log
git log –oneline
git revert <id-"from log">

git remote add origin https://github.com/user/repo.git
git push -u origin main
ssh-keygen -t rsa -C email
go to setting in GitHub addsshkey copy and paste the ssh key
git clone https://github.com/user/repo.git
git pull origin main

git init 
git branch -m main
git remote add origin
git add .
git commit -m “javapush”
git push -u origin main
